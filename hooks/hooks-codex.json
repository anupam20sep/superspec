{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "startup|resume",
        "hooks": [
          {
            "type": "command",
            "command": "echo SuperSpec: invoke the using-superspec skill before any task — explore/ingest → scope → refine → architect → plan → route → forge → validate → ship.",
            "statusMessage": "SuperSpec session start"
          }
        ]
      }
    ]
  }
}
