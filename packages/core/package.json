{
  "name": "@superspec-dev/core",
  "version": "0.1.1",
  "description": "Shared engine behind SuperSpec's spec-driven + TDD-driven lifecycle tools — exposes an MCP server and CLI for matrix, lint, scaffold, and persona discovery.",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/anupam20sep/superspec.git"
  },
  "main": "./dist/index.js",
  "bin": { "superspec": "./dist/cli.js" },
  "files": ["dist"],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -b",
    "start:mcp": "node dist/mcp-server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@superspec-dev/render": "^0.1.1",
    "zod": "^3.23.0"
  }
}
