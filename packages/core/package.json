{
  "name": "@superspec-dev/core",
  "version": "0.2.5",
  "description": "Shared engine behind SuperSpec's spec-driven + TDD-driven lifecycle — MCP server and CLI for init, matrix, lint, scaffold, forge loop, FR status sync, and persona discovery.",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/anupam20sep/superspec.git"
  },
  "main": "./dist/index.js",
  "bin": { "superspec": "bin/superspec.js" },
  "files": ["dist", "templates", "bin"],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -b && node scripts/ensure-bin-shebang.mjs",
    "start:mcp": "node dist/mcp-server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@superspec-dev/render": "^0.2.0",
    "zod": "^3.23.0"
  }
}
